spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/smartlift
    username: postgres
    password: test-db-password
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: test-jwt-secret-for-ci-only-000000000000
  expiration: 3600000
