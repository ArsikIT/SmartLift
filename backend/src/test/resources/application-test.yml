spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/smartlift
    username: postgres
    password: "0000"
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: integration-test-secret-key-that-is-at-least-32-chars
  expiration: 3600000
