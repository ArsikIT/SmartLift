# Database
DB_URL=jdbc:postgresql://localhost:5433/smartlift
DB_USERNAME=postgres
DB_PASSWORD=
DB_PORT=5433
POSTGRES_DB=smartlift

# JWT — generate secret with: openssl rand -base64 32
JWT_SECRET=
JWT_EXPIRATION=3600000

# App
APP_PORT=8080
